## DB/DBMS

| 名称   | 含义       | 举例           |
| ---- | -------- | ------------ |
| DB   | 数据库、数据集合 | 学生表、课程表、成绩表  |
| DBMS | 管理数据库的软件 | MySQL、Oracle |
| DBS  | 数据库系统    | DB+DBMS+应用程序 |

^90c474

## 数据抽象的三个层次（三级模式结构）

^aac975

- 外模式
- 概念模式
- 内模式
意思：同一份数据库，从不同角度看，看到的东西不一样
---
####  外模式（用户视图）= 用户看到的部分数据：
- 用户看到的数据样子
- eg. 学生只能看到自己的成绩
```Plain text
姓名
课程
成绩
```
但是看不到别人的成绩，也看不到后台的密码字段
- 标准定义（考试写法）
```Plain text
外模式是数据库用户能够看见和使用的局部数据的逻辑结构和特征描述。
```

---
#### 概念模式（全局逻辑结构）= 全体数据的逻辑结构：
- 数据库的总体逻辑结构
- 数据库增题长什么样
- 标准定义（考试写法）：
```Plain text
概念模式是数据库中全体数据的逻辑结构和特征描述
```

---
#### 内模式（存储模式） = 数据实际怎么存：
- 描述数据在磁盘上怎么存
- eg
```Plain text
索引
文件组织方式
存储路径
数据块
压缩方式
```