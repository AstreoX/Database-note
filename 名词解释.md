# 数据库期末：名词解释复习资料完整版

## Chapter 1 数据库系统

### 1. 数据（Data）
数据是数据库中存储的基本对象，是描述客观事物的符号记录，可以是数字、文字、图像、声音等。

### 2. 数据库（DB）
数据库（Database，DB）是长期存储在计算机内、有组织、可共享的大量数据的集合。

关键词：长期存储、有组织、可共享、数据集合。

### 3. 数据库管理系统（DBMS）
数据库管理系统（Database Management System，DBMS）是管理数据库的软件，用于数据库的定义、建立、维护、查询和控制。

### 4. 数据库系统（DBS）
数据库系统（Database System，DBS）是由数据库、数据库管理系统、应用程序和数据库用户组成的整体系统。

### 5. 数据抽象
数据抽象是隐藏数据存储细节，仅向用户提供所需数据表示的方法。

### 6. 外模式
外模式又称用户模式，是数据库用户看到和使用的局部数据逻辑结构。

### 7. 概念模式
概念模式又称模式，是数据库中全体数据的逻辑结构和特征描述。

### 8. 内模式
内模式又称存储模式，是数据库数据物理存储结构和存储方式的描述。

### 9. 数据独立性
数据独立性是指应用程序与数据库结构相互独立，不因数据库结构变化而修改应用程序。

### 10. 逻辑数据独立性
逻辑数据独立性是指概念模式改变时，外模式和应用程序尽量不受影响。

### 11. 物理数据独立性
物理数据独立性是指内模式改变时，概念模式和应用程序尽量不受影响。

---

## Chapter 2 关系模型

### 12. 关系数据库
关系数据库是采用关系模型组织数据的数据库，它以二维表的形式表示数据及数据之间的联系。

### 13. 关系（Relation）
关系是关系数据库中的一张二维表。

### 14. 元组（Tuple）
元组是关系中的一行数据，也称为一条记录。

### 15. 属性（Attribute）
属性是关系中的一列，用于描述对象某一方面的特征。

### 16. 域（Domain）
域是某个属性允许取值的集合。

### 17. 分量（Component）
分量是元组中某个属性对应的具体属性值。

### 18. 关系模式（Relation Schema）
关系模式是关系的结构描述，由关系名和属性名组成。

例：Student(ID, Name, Dept)

### 19. 关系实例（Relation Instance）
关系实例是某一时刻关系中实际存在的元组集合，也就是某一时刻表中的具体数据。

### 20. 模式（Schema）
模式是数据库结构的描述，包括数据的名称、类型、联系和约束等。

### 21. 实例（Instance）
实例是数据库在某一时刻存储的实际数据内容。

### 22. 超码（Super Key）
能唯一标识关系中一个元组的属性集称为超码。

特点：超码可以包含多余属性。

### 23. 候选码（Candidate Key）
不含多余属性的最小超码称为候选码。

特点：候选码是最小超码。

### 24. 主码（Primary Key）
从候选码中选定的一个作为唯一标识元组的码称为主码。

### 25. 候选码与主码的区别
候选码可以有多个，主码是从候选码中选定的一个。候选码都能唯一标识元组，但一个关系通常只选一个作为主码。

### 26. 主属性
包含在任一候选码中的属性称为主属性。

### 27. 非主属性
不属于任何候选码的属性称为非主属性。

### 28. 外码（Foreign Key）
一个关系中的属性或属性组引用另一个关系的主码，该属性或属性组称为外码。

### 29. 实体完整性
实体完整性要求主码不能为空且不能重复。

### 30. 参照完整性
参照完整性要求外码的取值要么为空，要么等于被参照关系主码中的某个值。

### 31. 用户自定义完整性
用户自定义完整性是用户根据具体应用需求定义的数据约束条件。

---

## Chapter 3 SQL

### 32. SQL
SQL（Structured Query Language）是关系数据库的标准语言，用于数据库的定义、查询、更新和控制。

### 33. 数据定义语言（DDL）
数据定义语言用于定义和修改数据库对象，如 CREATE、ALTER、DROP 等。

### 34. 数据操纵语言（DML）
数据操纵语言用于插入、删除和修改数据库中的数据，如 INSERT、DELETE、UPDATE 等。

### 35. 数据查询语言（DQL）
数据查询语言用于查询数据库中的数据，主要语句是 SELECT。

### 36. 空值（NULL）
NULL 表示未知值、缺失值或不适用的值，不等于 0，也不等于空字符串。

注意：判断空值要用 IS NULL 或 IS NOT NULL。

### 37. 聚集函数
聚集函数用于对一组数据进行统计计算，如 COUNT、SUM、AVG、MAX、MIN。

### 38. GROUP BY
GROUP BY 用于按照指定属性进行分组，以便对每组数据进行统计。

### 39. HAVING
HAVING 用于对分组后的结果进行条件筛选。

### 40. WHERE
WHERE 用于对表中的行进行条件筛选，发生在分组之前。

### 41. WHERE 与 HAVING 的区别
WHERE 用于分组前筛选行，HAVING 用于分组后筛选组。WHERE 后一般不能直接使用聚集函数作为筛选条件。

### 42. LIKE
LIKE 用于字符串模式匹配，常与通配符 % 和 _ 一起使用。

% 表示任意长度字符串，_ 表示任意一个字符。

### 43. 集合运算
集合运算是对两个查询结果进行运算，包括 UNION、INTERSECT、EXCEPT 等。

---

## Chapter 4 连接、视图和完整性约束

### 44. 笛卡尔积
两个关系中所有元组两两组合形成的新关系称为笛卡尔积。

### 45. 连接
连接是根据一定条件把两个关系中的相关元组组合起来形成新关系的操作。

### 46. 内连接
内连接仅保留两个关系中满足连接条件的元组。

### 47. 自然连接
自然连接是按照两个关系中的同名属性值相等进行连接，并去除重复属性的连接运算。

### 48. 内连接与自然连接的区别
内连接需要显式指定连接条件；自然连接自动按照同名属性进行等值连接，并去除重复属性。

### 49. 外连接
外连接是在普通连接基础上保留未匹配元组的一种连接方式，未匹配部分用 NULL 填充。

### 50. 左外连接
左外连接保留左表中的全部元组，右表没有匹配的部分用 NULL 填充。

### 51. 右外连接
右外连接保留右表中的全部元组，左表没有匹配的部分用 NULL 填充。

### 52. 全外连接
全外连接保留左右两个关系中的全部元组，匹配不成功的部分用 NULL 填充。

### 53. 视图（View）
视图是由一个或多个基本表导出的虚表，其数据来源于基本表，本身通常不存储数据。

关键词：虚表、来源于基本表、通常不存数据。

### 54. 基本表
基本表是数据库中实际存储数据的表，是独立存在的关系。

### 55. 视图与基本表的区别
基本表实际存储数据，视图通常不存储数据；视图是由基本表或其他视图通过查询定义出来的虚表，依赖于基本表。

### 56. 视图的作用
视图可以简化查询、隐藏数据细节、提高数据安全性，并为不同用户提供不同的数据视角。

---

## Chapter 6 关系代数

### 57. 关系代数
关系代数是一种以关系为运算对象、以关系为运算结果的形式化查询语言。

### 58. 选择（Selection）
选择是在关系中按照给定条件选取满足要求的元组。

对应 SQL：WHERE。

符号：σ

### 59. 投影（Projection）
投影是在关系中选取若干属性组成新的关系。

对应 SQL：SELECT。

符号：π

### 60. 并运算
并运算用于合并两个关系中的所有元组，并去除重复元组。

对应 SQL：UNION。

符号：∪

### 61. 交运算
交运算用于得到两个关系中共同拥有的元组。

对应 SQL：INTERSECT。

符号：∩

### 62. 差运算
差运算用于得到属于第一个关系但不属于第二个关系的元组。

对应 SQL：EXCEPT。

符号：−

### 63. 笛卡尔积运算
笛卡尔积运算是将两个关系中的元组两两组合形成新关系。

符号：×

### 64. 连接运算
连接运算是在笛卡尔积基础上按照连接条件筛选元组形成新关系。

符号：⋈

### 65. 重命名运算
重命名运算用于改变关系名或属性名，常用于避免同名冲突。

符号：ρ

---

## Chapter 7 E-R 模型

### 66. E-R 模型
E-R 模型是一种用于数据库概念设计的模型，通过实体、属性和联系描述现实世界。

### 67. 实体（Entity）
客观存在并能够相互区别的事物称为实体。

例如：学生、教师、课程。

### 68. 属性（Attribute）
描述实体特征的项目称为属性。

例如：学生的学号、姓名、年龄。

### 69. 联系（Relationship）
实体之间存在的关联称为联系。

例如：学生选修课程。

### 70. 实体集
同类实体的集合称为实体集。

例如：所有学生组成学生实体集。

### 71. 联系集
同类联系的集合称为联系集。

### 72. 一对一联系（1:1）
若一个实体集中的一个实体最多对应另一个实体集中的一个实体，反之也成立，则称为一对一联系。

### 73. 一对多联系（1:N）
若一个实体集中的一个实体可以对应另一个实体集中的多个实体，而另一方每个实体只对应前者一个实体，则称为一对多联系。

### 74. 多对多联系（M:N）
若两个实体集中的实体之间可以相互对应多个实体，则称为多对多联系。

### 75. 概念设计
概念设计是根据用户需求抽象出现实世界中的实体、属性和联系，并建立 E-R 模型的过程。

### 76. 逻辑设计
逻辑设计是将概念模型转换为某种数据模型的过程，例如将 E-R 图转换为关系模式。

### 77. 物理设计
物理设计是为数据库选择合适的存储结构和存取方法的过程。

---

## Chapter 8 关系数据库设计与范式

### 78. 数据冗余
数据冗余是指同一数据在数据库中被重复存储的现象。

### 79. 更新异常
更新异常是指由于数据冗余，修改数据时需要修改多处，若修改不一致会造成数据矛盾。

### 80. 插入异常
插入异常是指由于关系模式设计不合理，某些数据在缺少其他数据时无法插入。

### 81. 删除异常
删除异常是指由于关系模式设计不合理，删除某些数据时会连带删除本应保留的信息。

### 82. 规范化
规范化是通过分解关系模式来减少数据冗余、消除插入异常、删除异常和更新异常的过程。

### 83. 函数依赖
若属性组 X 的值能够唯一确定属性组 Y 的值，则称 Y 函数依赖于 X，记作 X→Y。

### 84. 平凡函数依赖
若 Y 是 X 的子集，则 X→Y 称为平凡函数依赖。

### 85. 非平凡函数依赖
若 Y 不是 X 的子集，则 X→Y 称为非平凡函数依赖。

### 86. 完全函数依赖
若 Y 函数依赖于 X，且去掉 X 中任何一个属性都不能确定 Y，则称 Y 完全函数依赖于 X。

### 87. 部分函数依赖
若 Y 函数依赖于组合属性 X，但只依赖于 X 的一部分属性，则称 Y 部分函数依赖于 X。

### 88. 传递函数依赖
若 X→Y，Y→Z，且 Y 不属于 X、Y 不能反向决定 X，则称 Z 传递函数依赖于 X。

### 89. 属性闭包
属性集 X 在函数依赖集 F 作用下能够推出的所有属性组成的集合称为 X 的属性闭包，记作 X⁺。

### 90. 函数依赖集闭包
函数依赖集 F 能够推出的所有函数依赖的集合称为 F 的闭包，记作 F⁺。

### 91. 第一范式（1NF）
关系中每个属性值都必须是不可再分的原子值，则该关系满足第一范式。

### 92. 第二范式（2NF）
在满足第一范式的基础上，每个非主属性都完全函数依赖于候选码，则该关系满足第二范式。

### 93. 第三范式（3NF）
在满足第二范式的基础上，消除了非主属性对候选码的传递函数依赖，则该关系满足第三范式。

### 94. BCNF
在满足第三范式的基础上，若每一个决定因素都是候选码，则该关系满足 BCNF。

### 95. 1NF、2NF、3NF、BCNF 的关系
1NF 要求属性原子性；2NF 消除部分函数依赖；3NF 消除传递函数依赖；BCNF 要求每个决定因素都是候选码。BCNF 比 3NF 更严格，满足 BCNF 一定满足 3NF，但满足 3NF 不一定满足 BCNF。

### 96. 决定因素
在函数依赖 X→Y 中，X 称为决定因素。

### 97. 码
码是能够唯一标识关系中一个元组的属性或属性组。

### 98. 最小覆盖
最小覆盖是与原函数依赖集等价、且右部为单属性、左部无多余属性、依赖无冗余的函数依赖集。

### 99. 无损连接分解
无损连接分解是指关系模式分解后，通过自然连接可以恢复原关系，不产生额外元组。

### 100. 保持函数依赖分解
保持函数依赖分解是指关系模式分解后，原有函数依赖仍能在分解后的关系模式中得到保持。

---

## 高频必背名词解释

### 第一梯队：最可能考
1. 数据库（DB）
2. 数据库管理系统（DBMS）
3. 数据库系统（DBS）
4. 关系模式
5. 超码
6. 候选码
7. 主码
8. 外码
9. 实体完整性
10. 参照完整性
11. SQL
12. 自然连接
13. 视图
14. 关系代数
15. E-R 模型
16. 函数依赖
17. 属性闭包
18. 第一范式（1NF）
19. 第二范式（2NF）
20. 第三范式（3NF）
21. BCNF

### 第二梯队：选择、判断、简答常见
1. 数据独立性
2. 外模式
3. 概念模式
4. 内模式
5. 关系数据库
6. 关系
7. 元组
8. 属性
9. 域
10. 关系实例
11. NULL
12. GROUP BY
13. HAVING
14. 笛卡尔积
15. 内连接
16. 外连接
17. 选择
18. 投影
19. 实体
20. 联系

### 第三梯队：范式题常见
1. 数据冗余
2. 更新异常
3. 插入异常
4. 删除异常
5. 规范化
6. 完全函数依赖
7. 部分函数依赖
8. 传递函数依赖
9. 决定因素
10. 最小覆盖
11. 无损连接分解
12. 保持函数依赖分解

---

## 速记口诀

### 数据库系统
DB 是数据集合，DBMS 是管理软件，DBS 是完整系统。

### 三级模式
外模式：用户怎么看。
概念模式：整体怎么组织。
内模式：底层怎么存。

### 关系模型
关系是表，元组是行，属性是列，域是取值范围。

### 码
超码能唯一，可以有多余。
候选码是最小超码。
主码是选中的候选码。
外码是引用别表主码。

### 完整性
实体完整性：主码非空且唯一。
参照完整性：外码必须引用存在的主码或为空。
用户自定义完整性：按业务规定约束。

### SQL
WHERE 筛行，HAVING 筛组。
GROUP BY 分组统计。
NULL 不能用 = 判断。

### 连接
笛卡尔积：全部乱配。
内连接：只要匹配。
自然连接：同名自动匹配。
左外连接：左边全要。
右外连接：右边全要。
全外连接：两边全要。

### 关系代数
σ 是选择，筛行，对应 WHERE。
π 是投影，选列，对应 SELECT。
⋈ 是连接。
∪ 是并。
∩ 是交。
− 是差。

### E-R 模型
实体用矩形，属性用椭圆，联系用菱形。
实体变表，属性变字段，M:N 联系单独变表。

### 范式
1NF：属性原子。
2NF：消除部分依赖。
3NF：消除传递依赖。
BCNF：决定因素都是候选码。

### 属性闭包
X⁺ 表示从 X 出发，根据函数依赖能推出的所有属性。
若 X⁺ 包含关系全部属性，则 X 是超码；若再无多余属性，则 X 是候选码。