## 2.1 关系数据库结构
```Plain text
关系数据库 = 用“表”来组织数据的数据库
```
- 关系 ≈ 一张二维表

|        |     |      |     |
| ------ | --- | ---- | --- |
| 学号     | 姓名  | 专业   | 年级  |
| 202401 | 张三  | 计算机  | 大二  |
| 202402 | 李四  | 软件工程 | 大二  |

这张表在数据库里就叫一个关系。
  
一张表里的几个概念

|      |             |                    |
| ---- | ----------- | ------------------ |
| 概念   | 通俗理解        | 例子                 |
| 关系   | 一张表         | 学生表                |
| 元组   | 表中的一行       | 202401，张三，计算机，大二   |
| 属性   | 表中的一列       | 学号、姓名、专业、年级        |
| 域    | 属性的取值范围     | 年级只能是大一、大二、大三、大四   |
| 关系实例 | 某一时刻表里的具体数据 | 现在学生表里的所有行         |
| 关系模式 | 表结构         | 学生(学号, 姓名, 专业, 年级) |

速记
```Plain text

表 = 关系

行 = 元组

列 = 属性

取值范围 = 域

表结构 = 关系模式
```
  

## 2.2 关系模式

关系模式就是表的“结构说明”。

格式一般写成：

关系名(属性1, 属性2, 属性3, ...)

比如：
```Plain text
Student(Sno, Sname, Major, Grade)
```
意思是：

Student 这张表有 Sno、Sname、Major、Grade 这些字段。

它只描述结构，不描述具体数据。

所以：

Student(Sno, Sname, Major)

是关系模式。

而：

|   |   |   |
|---|---|---|
|Sno|Sname|Major|
|001|张三|计算机|
|002|李四|数学|

是关系实例。

  

## 2.3 各种码

这是 Chapter 2 的重点。

先记住一句话：

码就是用来唯一确定一行数据的属性或属性组。

比如学生表：

|   |   |   |   |
|---|---|---|---|
|学号|身份证号|姓名|班级|
|001|370xxx|张三|计科1班|
|002|371xxx|李四|计科1班|

能唯一确定一个学生的有：

学号

身份证号

但姓名不行，因为可能重名。

---

① 超码

超码：能唯一确定一个元组的属性集。

只要能唯一定位一行，就是超码。

比如：

学号

身份证号

学号 + 姓名

学号 + 班级

身份证号 + 姓名

这些都是超码。

注意：
-  超码可以有多余属性。

比如学号本来就能唯一确定学生了，学号 + 姓名 也能唯一确定，但姓名是多余的。

所以：

- 超码 = 能唯一确定一行，可以有多余字段。

  
--- 
② 候选码

候选码：没有多余属性的超码。

也就是最小超码。

比如：

学号

身份证号

它们都能唯一确定学生，而且没有多余字段。

所以它们是候选码。

但是：

学号 + 姓名

不是候选码，因为学号已经够了，姓名是多余的。

所以：

候选码 = 最小的超码

  
---
③ 主码

主码：从候选码中选一个作为主要标识。

比如候选码有：

学号

身份证号

学校通常选：

学号

作为主码。

所以：

主码 ⊆ 候选码

考试常见说法：

一个关系可以有多个候选码，但只能选一个主码。

---
④ 外码

外码：一个表中的属性，引用另一个表的主码。

比如：

学生表：

Student(Sno, Sname)

课程表：

Course(Cno, Cname)

选课表：

SC(Sno, Cno, Grade)

其中：

SC.Sno 引用 Student.Sno

SC.Cno 引用 Course.Cno

所以：

SC.Sno 是外码

SC.Cno 是外码

外码的作用：

让两张表之间建立联系。

  

各种码之间的关系

你可以这么理解：

超码：能唯一确定一行，允许多余

候选码：能唯一确定一行，不能多余

主码：从候选码里挑一个

外码：引用别的表主码，用来建立联系

再画成层级：

超码

 └── 候选码

      └── 主码

外码不在这个层级里，它是“连接别的表”的。

  

## 完整性约束

完整性约束就是：

数据库为了保证数据正确性而设置的规则。

你大纲里有三个：

实体完整性

参照完整性

用户自定义完整性

  
---
① 实体完整性

核心：

主码不能为空，且不能重复。

比如学生表：

|   |   |
|---|---|
|学号|姓名|
|001|张三|
|001|李四|

不行，因为学号重复。

|   |   |
|---|---|
|学号|姓名|
|null|张三|

也不行，因为主码为空。

考试写法：
```Plain text
实体完整性要求关系的主码属性不能为空，且主码值能够唯一标识元组。
```
---

② 参照完整性

核心：

外码要么为空，要么必须引用已经存在的主码。

比如学生表：

|   |   |
|---|---|
|Sno|Sname|
|001|张三|
|002|李四|

选课表：

|   |   |   |
|---|---|---|
|Sno|Cno|Grade|
|001|C01|90|
|003|C02|80|

这里 003 不存在于学生表，所以违反参照完整性。

考试写法：

参照完整性要求外码的取值要么为空，要么等于被参照关系中某个主码值。

  

③ 用户自定义完整性

核心：

根据具体业务自己规定的约束。

比如：

成绩必须在 0 到 100 之间

年龄必须大于 0

性别只能是 男 / 女

邮箱格式必须合法

工资不能低于最低工资

考试写法：

用户自定义完整性是针对具体应用环境，由用户定义的数据约束条件。

  

Chapter 2 常考题型

题型 1：判断各种码

给一个关系：

Student(Sno, IDcard, Sname, Class)

已知：

Sno 唯一

IDcard 唯一

Sname 不唯一

Class 不唯一

问：

超码、候选码、主码可以是什么？

答：

超码：Sno，IDcard，Sno+Sname，Sno+Class，IDcard+Sname 等

候选码：Sno，IDcard

主码：可以选择 Sno 或 IDcard，一般选 Sno

  

题型 2：判断外码

Student(Sno, Sname)

Course(Cno, Cname)

SC(Sno, Cno, Grade)

问：

SC 中哪些是外码？

答：

Sno 是外码，引用 Student(Sno)。

Cno 是外码，引用 Course(Cno)。

  

题型 3：判断完整性约束

问：

学生成绩必须在 0 到 100 分之间属于什么完整性？

答：

用户自定义完整性。

问：

学生表中学号不能为空属于什么完整性？

答：

实体完整性。

问：

选课表中的学号必须来自学生表属于什么完整性？

答：

参照完整性。

  

这一章你必须背下来的东西

关系 = 表

元组 = 行

属性 = 列

域 = 取值范围

关系模式 = 表结构

关系实例 = 某一时刻表中的数据

  

超码 = 能唯一确定元组的属性集，可以有多余属性

候选码 = 最小超码

主码 = 从候选码中选出的一个

外码 = 引用其他关系主码的属性

  

实体完整性 = 主码不能为空且不能重复

参照完整性 = 外码要么为空，要么引用已存在的主码

用户自定义完整性 = 用户根据实际业务定义的约束

下一模块建议直接进入 Chapter 3 SQL，这是最重要、最容易出大题的部分。