```
1NF
↓

2NF
↓

3NF
↓

BCNF
↓

规范化理论

↓

图8-8 属性闭包

↓

属性闭包算法
```
# Part 1. 为什么要有范式？
- 减少数据冗余（重复）
- 避免更新异常
- 避免插入、删除异常
# Part 2.  什么是函数依赖？
### 定义：
记号：
```
X → Y
```
读作：
```
X 决定 Y
```
或者：
```
Y 函数依赖于 X
```
eg.
有一个学生表

| 学号  | 姓名  |
| --- | --- |
| 001 | 张三  |
| 002 | 李四  |
看到`学号=001` 马上就能知道`姓名=张三`

所以
```
学号 → 姓名
```
意思就是：

>知道学号，就唯一确定姓名
# Part 3. 什么是完全函数依赖
假设一张选课表，有：
- 学号
- 课程号
- 成绩
那么必须：`学号+课程号`一起，才能唯一确定`成绩`
即`(学号,课程号)→成绩`
这就是*完全函数依赖*
>必须整个主键一起才能决定某属性
# Part 4. 部分函数依赖
还是刚才的`(学号,课程号)`
如果决定姓名的话，可以直接*学号*即可，不需要*课程号*
那么说明*课程号*是多余的
于是`(学号,课程号)→姓名`
属于*部分函数依赖*

---
# 范式（Normal Form）
整体关系：
```
1NF
↓
解决最基本的数据规范

2NF
↓
消灭部分函数依赖

3NF
↓
消灭传递函数依赖

BCNF
↓
消灭所有非候选码决定别人
```
# 第一范式（1NF）
>每个属性必须是不可再分的原子值

*一列只能放一个值*
# 第二范式（2NF）
>所有非主属性
 必须完全依赖主键。

怎么解决？

*拆表*
# 第三范式（3NF）
>解决传递函数依赖

*不能出现 非主属性 决定 非主属性。*

# BCNF
> 每一个决定因素，都必须是候选码

***

# 属性闭包
- 判断是不是候选码
- 判断函数依赖是否成立
- 范式分解
## 怎么判断是不是候选码
题目：
```
有关系：
R(A,B,C,D)

函数依赖：
A → B
B → C
C → D
```
问：`A是不是候选码？`

*思路*
计算A⁺
>不断利用函数依赖，把能推出的属性全部加入
1. 先把自己放进去
	A⁺={A}
	
	知道 A
	当然知道 A 自己。
2. 看函数依赖：
	发现A→B
	已经有A，可以推出Ｂ
	于是有
	A⁺={A,B}
3. 到最后
	发现：*A⁺={A,B,C,D}*
	而且 = R(A,B,C,D) 全部属性
所以A能决定整个关系，所以A是候选码

## 判断函数依赖
题目：
```
已知：
A→B
B→C
问：
A→C
成立吗？
```
*思路*
计算A⁺，得到A⁺={A,B,C}，因为里面有C，所以A→C成立